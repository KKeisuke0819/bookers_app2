<h1>Books</h1>

<table class="table table-striped">
  <tr>
    <th></th>
    <th>Title</th>
    <th>Opnion</th>
    <th></th>
    <th></th>
  </tr>

  <% books.each do |book| %>
    <tr>
      <td>
        <%= link_to user_path(book.user.id) do %>
        <%= attachment_image_tag book.user, :profile_image, :fill, 100, 100, fallback: "no_image.jpeg" %>
        <% end %>
      </td>
      <td><%= link_to book.title, book_path(book.id) %></td>
      <td><%= book.body %></td>
      <!-- <td><%= link_to '詳細', "#" %></td>
      <td><%= link_to '編集', "#" %></td>
      <td><%= link_to '削除', "#",
                method: :delete, data: { confirm: '削除しますか？' } %></td> -->
    </tr>
  <% end %>
</table>
